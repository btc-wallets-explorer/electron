How to build:

```bash
npm install
npm run install
npm run build
npm run compile
npm run dist
```

How to run:
```bash
/path/to/executable --wallets=/path/to/wallets.json --settings=/path/to/settings.json
```
